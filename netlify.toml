# Netlify config para Next.js (SSR)
# Build: next build. El plugin @netlify/plugin-nextjs (OpenNext) corre después y genera
# el output que Netlify publica. NO pongas "Publish directory" en la UI (dejalo vacío).

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
